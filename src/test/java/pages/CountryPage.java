package pages;

import io.cucumber.java.zh_cn.假如;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindBy;
import org.openqa.selenium.support.PageFactory;
import utilities.Driver;

public class CountryPage {
    public CountryPage() {
        PageFactory.initElements(Driver.getDriver(), this);
    }






























































































































































    //Aykut 171-220
    @FindBy(xpath = "//*[text()='Create a new Country']")
    public WebElement createANewCountryButton;

    @FindBy(xpath = "(//*[text()='Delete'])[21]")
    public WebElement deleteButtonInCountry;

    @FindBy(xpath = "//*[text()='Cancel']")
    public WebElement cancelInCountryPage;

    @FindBy(xpath = "//*[text()='A Country is deleted with identifier 83248']")
    public WebElement kontrollKätchenACountryIsDeletedWithIdentifier;






























}
